{
  "name": "test-subgraph-001",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ test-subgraph-001",
    "create-local": "graph create --node http://localhost:8020/ test-subgraph-001",
    "remove-local": "graph remove --node http://localhost:8020/ test-subgraph-001",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 test-subgraph-001",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.68.1",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
